# Twilio credentials for server/ ONLY. Never commit real values.
# Copy to server/.env (or root .env) and fill in real values locally.
TWILIO_SID=
TWILIO_TOKEN=
TWILIO_FROM=
ALERT_TO=
# Optional: backend port (default 5000)
PORT=
